// Package fixtures: sample-repo is a tiny Go module with a deliberate bug
// (calculator.Sub adds instead of subtracting) and a failing test. The
// agent end-to-end test drives the full loop against a copy of it.
package fixtures
